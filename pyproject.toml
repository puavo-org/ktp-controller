[project]
name = "ktp-controller"
version = "0.5.0rc1"
description = "ktp-controller"
authors = [{ name = "Opinsys Developers", email = "dev@opinsys.fi" }]
requires-python = ">=3.11, <3.15"
classifiers = [
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
readme = "README.md"
license = "GPL-2.0-only"
maintainers = [{ name = "Opinsys Developers", email = "dev@opinsys.fi" }]
dependencies = [
    "asyncinotify",
    "fastapi",
    "httpx",
    "uvicorn[standard]",
    "alembic",
    "psutil",
    "pydantic-settings",
    "python-multipart",
    "supervisor",
    "redis",
    "validators",
    "websockets",
]
repository = "https://github.com/puavo-org/ktp-controller"

[dependency-groups]
dev = [
    "pytest",
    "pytest-mock",
    "httpx",
    "selenium",
    "ruff>=0.14.13",
]

[tool.uv.build-backend]
module-root = ""
module-name = "ktp_controller"

[build-system]
requires = ["uv_build>=0.9.0,<0.10.0"]
build-backend = "uv_build"
